package com.globmart.util;

import com.globmart.domain.Product;

import java.util.ArrayList;
import java.util.List;

public class UserUtil {
/*

    private static final String ID = "id";
    private static final String PASSWORD = "password";

    private UserUtil() {
    }

    public static Product createUser() {
        return new Product(ID, PASSWORD);
    }

    public static List<Product> createUserList(int howMany) {
        List<Product> productList = new ArrayList<>();
        for (int i = 0; i < howMany; i++) {
            productList.add(new Product(ID + "#" + i, PASSWORD));
        }
        return productList;
    }
*/

}
